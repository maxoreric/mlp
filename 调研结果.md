# 沉浸式翻译（Immersive Translate）开源复刻项目深度技术调研报告与架构规格书

## 1. 执行摘要与产品需求文档 (PRD)

### 1.1 产品愿景与核心理念

本技术规格文档旨在定义一个能够精确复刻「沉浸式翻译」（Immersive Translate）核心功能的开源浏览器扩展项目的技术蓝图。该产品的核心价值主张在于「上下文对照翻译」（Contextual Bilingualism），即在保留原始网页视觉层级、布局和排版的基础上，将翻译内容以段落为单位无缝嵌入到原文之后或之下 。

与传统的全文替换式翻译（如 Google Chrome 自带的翻译功能）不同，本项目的目标受众——语言学习者、跨境科研人员及外贸从业者——更看重信息的准确性与双语对照的即时校验能力。产品必须在「低侵入性」与「高可读性」之间取得极致平衡，确保在翻译的同时不破坏宿主网页的 DOM 结构和交互逻辑 。

### 1.2 MVP (最小可行性产品) 范围界定

基于开源复刻的目标，MVP 阶段将严格聚焦于以下四大核心支柱，以确保开发资源的高效利用并快速交付可用版本：

1. **Chrome 扩展基础架构 (Manifest V3)**：构建基于最新 Manifest V3 标准的扩展框架，采用 Service Worker 处理后台逻辑，确保符合 Chrome Web Store 的安全与性能规范 。


2. **智能网页双语对照翻译**：实现一套高鲁棒性的 DOM 解析引擎，能够智能识别网页的「主要内容区域」，排除导航栏、广告等干扰元素，并以 Shadow DOM 技术注入双语对照样式 。


3. **流媒体双语字幕注入 (YouTube/Bilibili)**：针对主流视频平台开发专用的字幕拦截与渲染模块，通过 Hook 播放器 API 或拦截网络请求（`webRequest`）获取字幕流，并实现时间轴同步的双语字幕叠加 。


4. **翻译引擎适配层**：构建模块化的翻译服务接口，MVP 阶段需支持免费的翻译源（如 Google/Bing 的 Web 接口封装）以及标准 OpenAI 兼容接口（支持 OpenAI, DeepSeek, Azure 等），以满足不同层次用户的需求 。



### 1.3 用户故事与验收标准

| 角色 | 典型场景 | 用户操作 | 系统响应 (验收标准) | 技术触点 |
| --- | --- | --- | --- | --- |
| **科研人员** | 阅读英文论文网页 | 点击扩展图标开启翻译 | 网页正文段落下方出现中文翻译，字体略小，颜色变灰，原文布局未乱。 | 内容提取算法, 样式注入 |
| **外语学习者** | 观看 YouTube 英文演讲 | 视频只有自动生成字幕 | 播放器底部出现双语字幕，英文在上，中文在下，随进度条精准同步。 | 字幕流拦截, 视频覆盖层 (Overlay) |
| **开发者/极客** | 使用自定义 LLM | 配置 DeepSeek API | 在设置页输入 API Key 和 Endpoint，翻译请求通过该接口发送并流式返回。 | API 适配器, 流式响应处理 |
| **普通用户** | 局部翻译 | 鼠标悬停在某一段落 | 按下快捷键（如 Shift）后，仅该段落下方立即显示翻译结果。 | 鼠标事件监听, 局部 DOM 突变 |
| **社交媒体用户** | 输入框辅助 | 在 Twitter 输入框打字 | 输入中文后连按三次空格，内容自动变为英文。 | 输入事件监听, 值替换逻辑 |

---

## 2. Chrome 扩展技术架构 (Manifest V3) 设计

为了确保项目的长期生命周期和安全性，本项目将严格遵循 Chrome Manifest V3 规范。这要求架构设计必须适应 Service Worker 的非持久化特性，并采用基于事件驱动的编程模型。

### 2.1 总体架构图与组件交互

系统由四个核心执行环境组成，它们之间通过 Chrome Messaging API 进行异步通信。

1. **UI 层 (Popup & Options Page)**：
* 采用 **React** 或 **Vue 3** 构建，追求轻量化。
* 负责用户交互、状态切换（开启/关闭）、API Key 管理和高级规则配置。
* 数据持久化通过 `chrome.storage.sync`（跨设备同步配置）和 `chrome.storage.local`（缓存翻译结果）实现。


2. **后台服务层 (Background Service Worker)**：
* 作为系统的中枢神经，不持有 DOM 访问权。
* **职责**：
* **API 代理**：所有跨域翻译请求（如调用 OpenAI API）必须在此发起，以规避内容脚本（Content Script）中的 CORS 限制。
* **上下文菜单管理**：处理右键菜单的创建与点击事件 。


* **网络拦截**：使用 `chrome.webRequest` 或 `chrome.declarativeNetRequest` 监听视频字幕文件的网络请求 。




* **生命周期管理**：由于 Service Worker 会在闲置时终止，必须利用 `chrome.storage` 进行状态恢复。


3. **内容脚本层 (Content Scripts - Isolated World)**：
* 运行在网页的隔离环境中，能够读取和修改 DOM，但无法访问网页的 JavaScript 变量（如 `window.player`）。
* **职责**：
* DOM 结构分析与主要内容提取。
* 文本分割与占位符处理。
* Shadow DOM 容器的注入与渲染。
* 监听用户交互（悬停、快捷键）。




4. **注入脚本层 (Injected Script - Main World)**：
* 通过 `<script>` 标签动态注入到页面中。
* **职责**：主要用于视频站点的 Hook。例如，直接访问 Bilibili 播放器的内部状态，或者劫持 `XMLHttpRequest.prototype.open` 来获取某些加密参数 。





### 2.2 安全策略与权限声明 (Manifest.json)

`manifest.json` 是扩展的配置文件，必须最小化权限申请以通过应用商店审核，同时满足功能需求。

**核心配置规格：**

```json
{
  "manifest_version": 3,
  "name": "Immersive Translate OSS",
  "version": "1.0.0",
  "permissions":
    "webRequest",       // 必选：用于拦截 YouTube/Bilibili 字幕请求 [7]
    "debugger"          // 可选：开发阶段用于调试复杂的网络流
  ],
  "host_permissions": [
    "<all_urls>"        // 必选：核心功能是翻译任意网页，必须申请所有主机权限
  ],
  "background": {
    "service_worker": "background.js"
  },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "run_at": "document_idle", // 页面加载空闲时执行，减少对首屏性能影响
      "all_frames": true         // 支持 iframe 内部翻译
    }
  ],
  "web_accessible_resources": [
    {
      "resources": ["injected.js", "assets/icons/*", "styles/*.css"],
      "matches": ["<all_urls>"]  // 允许任何页面加载扩展内的资源
    }
  ],
  "action": {
    "default_popup": "popup.html"
  }
}

```

技术依据：

### 2.3 消息通信机制设计

在 MV3 中，Content Script 与 Background 的通信是异步的。为了处理翻译请求的高并发与长延迟（LLM 生成速度慢），需设计一套基于 **Request ID** 的回调机制。

* **通信协议**：
```typescript
interface MessagePayload {
  action: 'TRANSLATE_BATCH' | 'GET_CONFIG' | 'SAVE_RULE';
  data: any;
  requestId?: string; // 用于匹配异步响应
}

```


* **流式传输支持**：对于 OpenAI 等支持 Stream 的接口，Background 需要通过 `chrome.tabs.sendMessage` 持续向 Content Script 发送 `TRANSLATE_CHUNK` 消息，以实现打字机效果，提升用户体验 。



---

## 3. 网页双语对照翻译引擎 (核心模块)

这是产品的灵魂功能。实现难点在于如何准确识别网页的“正文”，并在不破坏布局的前提下插入译文。

### 3.1 智能文档解析与内容提取算法

简单的遍历 `document.body` 会导致导航栏、广告、页脚等无关内容被翻译，破坏阅读体验。我们需要实现类似 Firefox Reader View (Mozilla Readability) 的启发式算法，但目标不是提取纯文本，而是标记“待翻译块” 。

**算法流程详解：**

1. **黑名单过滤 (Denylist Filtering)**：
* 在遍历 DOM 树前，直接剔除无需翻译的标签：`SCRIPT`, `STYLE`, `NOSCRIPT`, `CODE`, `PRE` (视配置而定), `SVG`, `INPUT`, `TEXTAREA`。
* 剔除具有特定 class/id 特征的元素：`nav`, `header`, `footer`, `ad`, `banner`, `sidebar`, `cookie-notice` 。




2. **块级元素评分 (Block Scoring)**：
* 遍历所有块级元素（`DIV`, `P`, `ARTICLE`, `SECTION`）。
* **评分维度**：
* **文本密度**：文本节点字符数 / 标签总数。
* **链接密度**：链接文本字符数 / 总字符数。高链接密度通常意味着导航栏或推荐列表，应降低权重 。


* **标点符号密度**：正文通常包含较多的逗号和句号。


* **结果**：筛选出一组“主要内容容器”。


3. **智能分段 (Smart Segmentation)**：
* 对于选定的容器，提取其直接子文本节点。
* **合并逻辑**：如果连续的文本节点被行内元素（如 `<a>`, `<b>`, `<i>`）隔开，应将其视为一个完整的段落进行翻译，而不是拆分成碎片。
* **占位符保护 (Tag Preservation)**：在发送给翻译引擎前，将 HTML 标签替换为特殊占位符（如 `{{0}}`, `{{1}}`），确保翻译后能还原 DOM 结构 。




*示例：*
* 原文 DOM: `<p>Welcome to <b>Immersive Translate</b> world.</p>`
* 发送给 LLM: `Welcome to {{0}} world.` (附带映射表: `{{0}} -> <b>Immersive Translate</b>`)
* LLM 返回: `欢迎来到 {{0}} 的世界。`
* 还原 DOM: `<p>...<br/><span class="trans">欢迎来到 <b>Immersive Translate</b> 的世界。</span></p>`



### 3.2 样式注入与 Shadow DOM隔离

为了防止宿主网页的 CSS（如全局 `p { color: red }`）污染译文样式，或者扩展的 CSS 影响原网页，**Shadow DOM** 是最佳的隔离方案 。

**实现规格：**

* **容器构建**：在每个被翻译的块级元素末尾插入一个 `<immersive-translate-wrapper>` 自定义元素。
* **Shadow Root**：调用 `attachShadow({ mode: 'closed' })`。
* **样式注入**：在 Shadow Root 内部注入一套独立的 CSS Reset 和主题样式。
* **主题支持**：
* **下划线模式**：原文下方显示虚线，悬停显示译文。
* **对照模式**：原文下方块级显示译文（默认）。
* **模糊模式**：译文默认模糊，鼠标悬停清晰（用于学习）。
* *技术实现*：通过在 Host 元素上切换 class（如 `.theme-underline`, `.theme-block`）来控制 CSS 变量。



### 3.3 视口感知与懒加载 (Performance Optimization)

一次性翻译长网页（如维基百科）会消耗大量 Token 并导致页面卡顿。必须引入 **IntersectionObserver**。

* **机制**：仅当“待翻译块”进入视口（Viewport）或即将进入视口（RootMargin 设置为 200px）时，才触发翻译请求。
* **缓存**：已翻译的段落 ID 存入 `WeakMap` 或 DOM 属性，避免滚动回看时重复翻译。

---

## 4. 视频双语字幕系统 (YouTube & Bilibili)

视频翻译的技术路径与网页文本完全不同，主要依赖于对字幕流数据的拦截、解析和时间轴同步渲染。

### 4.1 通用字幕渲染层 (Subtitle Overlay)

由于不同视频网站的播放器 DOM 结构差异巨大，我们需要构建一个通用的 UI 层。

* **定位策略**：通过 Content Script 找到视频容器（如 `video` 标签的父级 `div`），将一个绝对定位的 `div`（`#immersive-subtitle-layer`）覆盖在视频画面之上。
* **交互穿透**：设置 CSS `pointer-events: none`，确保用户点击字幕区域时，操作能传递给下方的视频播放器（暂停/播放）。
* **样式自定义**：支持用户调整字号、背景色、垂直位置（避免遮挡关键画面）。



### 4.2 YouTube 字幕技术实现

YouTube 的字幕机制较为成熟，主要通过 `timedtext` API 传输。

1. **字幕拦截 (The Hook)**：
* 监听 `chrome.webRequest.onBeforeRequest`，匹配模式 `*://www.youtube.com/api/timedtext*` 。


* **关键参数解析**：
* `v`: Video ID。
* `lang`: 字幕语言代码（如 `en`）。
* `fmt`: 格式（通常请求 `json3` 或 `vtt` 以便解析）。


* **主动获取**：如果用户开启了“自动翻译”，但视频未加载字幕，扩展需主动向 `timedtext` 接口发起请求，尝试获取 `lang=en` 的字幕数据。


2. **数据处理**：
* YouTube 返回的 JSON 数据包含 `events` 数组，每个事件有 `tStartMs`（开始时间）和 `dDurationMs`（持续时间）。
* 扩展需将这些片段缓存到内存中的 `SubtitleManager`。


3. **同步机制**：
* 通过 `requestAnimationFrame` 或 `timeupdate` 事件监听 `<video>` 元素的 `currentTime`。
* 二分查找算法快速定位当前时间对应的字幕片段，并渲染到 Overlay 层。


4. **AI 字幕生成 (Pro 功能复刻)**：
* 对于没有 CC 字幕的视频，YouTube 使用 ASR（自动语音识别）。扩展需请求 `lang=en&kind=asr` 的字幕流。
* 将 ASR 的碎片化文本（Word-level timestamps）通过算法合并为完整的句子，再发送给翻译引擎。



### 4.3 Bilibili 字幕技术实现 (难点攻关)

Bilibili 的技术栈较为复杂，涉及 Protobuf 协议和 GRPC 接口，且反爬虫策略较严 。

1. **API 逆向与 Protobuf 解码**：
* Bilibili 现代播放器通常从 `api.bilibili.com/x/player/v2` 或类似接口加载字幕。响应体往往是二进制的 Protobuf 数据。
* **解决方案 A (高技术门槛)**：
* 在扩展中集成 `protobufjs`。
* 根据开源社区（如  提及的 `XiaoMiku01/bilibili-grpc-api-go`）提供的 `.proto` 定义文件，生成解码器。


* 拦截响应流，解码二进制数据为 JSON 对象。


* **解决方案 B (降级策略 - 推荐 MVP)**：
* 利用 Bilibili 的 Web 接口回退机制。大多数视频仍可以通过 `https://api.bilibili.com/x/player/wbi/v2?cid={cid}&aid={aid}` 获取字幕列表（JSON 格式）。


* 该接口返回的 `subtitle.url` 指向一个 JSON 文件，直接 fetch 该文件即可规避 Protobuf 解码的复杂性。




2. **CID 获取**：
* `cid` (Content ID) 是获取字幕的关键。它通常嵌入在页面的 `window.__INITIAL_STATE__` 对象中。
* 使用 "Injected Script" (Main World) 读取 `window.__INITIAL_STATE__.videoData.cid`，并通过 `window.postMessage` 发送给 Content Script。


3. **弹幕与字幕的区分**：
* 需明确区分“CC 字幕”（Closed Captions）和“弹幕”（Danmaku）。本项目仅针对 CC 字幕进行翻译。弹幕翻译涉及复杂的俚语处理，不建议纳入 MVP。



---

## 5. 翻译引擎集成与 API 适配

为了实现“OpenAI 兼容”，我们需要构建一个标准化的请求适配器模式（Adapter Pattern）。

### 5.1 适配器接口定义

```typescript
interface TranslationAdapter {
  providerId: string;
  translate(texts: string, sourceLang: string, targetLang: string): Promise<string>;
  validateConfig(config: any): Promise<boolean>;
}

```

### 5.2 OpenAI 兼容接口实现

这是最核心的高级功能，支持 OpenAI, DeepSeek, Azure OpenAI 等。

* **API 端点**：用户可自定义（如 `https://api.deepseek.com/chat/completions`）。
* **请求构造**：
* `model`: 用户可配置（如 `gpt-3.5-turbo`, `deepseek-chat`）。
* `messages`: 构造 System Prompt 和 User Prompt。
* `temperature`: 建议设为 `0` 以保证翻译稳定性。


* **批处理 (Batching)**：
* 为了节省 Token 和网络开销，严禁逐句请求。
* **合并策略**：将 10-20 个段落（或总计 1000 字符以内）合并为一个 JSON 数组发送。
* **Prompt 示例**：
> "You are a professional translator. Translate the following JSON array of text segments from {source} to {target}. Keep the JSON structure unchanged. Do not translate code blocks or proper nouns marked with {{x}}."
> Input: `["Hello world", "Chapter 1"]`
> Output: `["你好世界", "第一章"]`





### 5.3 免费翻译源 (Web Scraper)

* **Google Translate (GTX)**：
* Endpoint: `https://translate.googleapis.com/translate_a/single?client=gtx&dt=t&...`
* 注意：该接口有 IP 频率限制。需在 UI 提示用户“仅供轻量使用，建议配置 API Key”。


* **Bing Translator**：
* 需动态获取 `IG` 和 `IID` 令牌（Token），通常需要请求一次 Bing 翻译主页来提取这些参数。



### 5.4 错误处理与重试机制

* **指数退避 (Exponential Backoff)**：遇到 429 (Too Many Requests) 错误时，自动等待 `2^n` 秒后重试。
* **故障转移 (Failover)**：如果首选引擎（如 OpenAI）失败，可自动降级到免费引擎（如 Google），并在 UI 显示警告。

---

## 6. 用户界面 (UI) 与交互设计详细规格

### 6.1 悬浮球 (Floating Ball)

悬浮球是用户触发翻译的高频入口，必须轻量且不遮挡内容。

* **实现技术**：Shadow DOM (避免被页面 CSS 覆盖)。
* **交互逻辑**：
* **单击**：触发当前视口内容的翻译。
* **双击**：切换“原文/双语/译文”显示模式。
* **拖拽**：支持在屏幕边缘吸附。
* **进度环**：在圆球周围绘制 SVG 环形进度条，实时显示当前页面的翻译进度（已翻译段落数 / 总段落数）。





### 6.2 弹出面板 (Popup)

* **当前状态卡片**：显示当前域名、是否启用翻译、当前使用的翻译服务。
* **快捷设置**：
* 源语言/目标语言选择。
* “总是翻译该网站”开关。
* “翻译全页”按钮（触发强制全量翻译）。



### 6.3 开发者设置页 (Options Page)

* **JSON 配置编辑器**：
* 这是 Immersive Translate 的强大之处。允许用户直接编辑 JSON 来定义规则 。


* **配置结构**：
```json
{
  "rules": [
    {
      "matches": ["twitter.com"],
      "selectors": [".tweet-text"],
      "excludeSelectors": [".user-name"]
    }
  ],
  "translationServices": {
    "openai": {
      "endpoint": "...",
      "apiKey": "sk-..."
    }
  }
}

```


* **UI 实现**：集成 `monaco-editor` (VS Code 编辑器内核) 提供 JSON 语法高亮和校验。



### 6.4 输入框增强 (Input Translation)

* **触发机制**：监听 `input` 和 `textarea` 元素的 `keydown` 事件。检测到连续三次按下 `Space` 键（或自定义快捷键）。


* **逻辑**：
1. 获取输入框当前的 `value`。
2. 显示“正在翻译...”的 Loading 状态（可在输入框右下角注入小图标）。
3. 调用翻译 API（通常是反向翻译，如中->英）。
4. 将翻译结果替换原输入框的内容。



---

## 7. 测试驱动开发 (TDD) 计划

为了保证代码质量，所有核心逻辑必须先写测试用例。

### 7.1 单元测试 (Unit Tests - Jest)

* **DOM 解析器测试**：
* *输入*：包含嵌套标签的 HTML 字符串 `<p>Hello <a>World</a></p>`。
* *预期*：提取文本 `Hello {{0}}`，并生成准确的还原映射表。


* **配置合并测试**：
* *输入*：全局规则 `{ exclude: ['nav'] }`，特定网站规则 `{ exclude: ['.menu'] }`。
* *预期*：合并后的规则应包含 `['nav', '.menu']`。


* **YouTube URL 解析测试**：
* *输入*：各种变体的 `timedtext` URL。
* *预期*：准确提取 `v`, `lang`, `fmt` 参数。



### 7.2 集成测试 (Integration Tests)

* **API 适配器测试**：
* 使用 `nock` 模拟 OpenAI API 的 200 OK, 401 Unauthorized, 429 Rate Limit 响应。
* 验证 Adapter 是否正确处理重试和错误抛出。



### 7.3 端到端测试 (E2E - Playwright/Puppeteer)

* **场景 1：网页翻译流程**
* 加载一个静态的英文 HTML 测试页。
* 模拟点击扩展图标。
* 断言：页面中是否出现了 `<immersive-translate-wrapper>` 元素？Shadow DOM 内是否有译文文本？


* **场景 2：Bilibili 字幕注入**
* Mock Bilibili 的字幕接口响应（返回预设的 JSON）。
* 加载视频页。
* 断言：播放器上方是否渲染了字幕遮罩层？字幕内容是否随 `video.currentTime` 变化？



---

## 8. 实施路线图与技术难点总结

### 8.1 阶段规划

1. **Phase 1 (基础架构)**：搭建 MV3 框架，实现 Popup 和 Options 设置页，打通 OpenAI API 链路。
2. **Phase 2 (网页翻译)**：实现 DOM 解析算法、Shadow DOM 注入、IntersectionObserver 优化。
3. **Phase 3 (视频模块)**：攻克 YouTube `timedtext` 和 Bilibili API 接入，实现字幕 Overlay。
4. **Phase 4 (高级功能)**：实现输入框增强、PDF 基础支持（可选）、悬浮球 UI。

### 8.2 关键风险与应对

* **风险**：Bilibili/YouTube 接口升级导致字幕获取失效。
* *应对*：将解析逻辑从核心代码剥离为“规则脚本”，支持通过远程 JSON 动态更新解析规则，无需重新发布扩展。


* **风险**：DOM 结构复杂导致翻译破坏布局。
* *应对*：完善“黑名单”库，提供“仅翻译文本模式”（不注入 UI，仅替换文本）作为兜底方案。


* **风险**：LLM 响应过慢。
* *应对*：实现流式输出（Streaming），让用户感觉到翻译是即时的；实现激进的本地缓存策略（IndexedDB）。



本技术规格文档详细涵盖了从架构设计、核心算法到具体的 API 逆向工程细节，为开发团队提供了一个清晰、可执行的开源复刻指南。通过严格遵循此文档，LLM 可辅助生成各模块的具体代码实现。